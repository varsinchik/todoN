import { ElementLoader } from './element-loader';

describe('ElementLoader', () => {
  it('should create an instance', () => {
    const directive = new ElementLoader();
    expect(directive).toBeTruthy();
  });
});
